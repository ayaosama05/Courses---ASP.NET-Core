@{
    ViewData["Title"] = "Home Page";
}

<!-- Modal Container-->
<div class="modal fade" id="UserLoginModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
    <partial name="_UserLoginPartial" />
</div>


<div class="row">
    <div class="col-md-3">
        @if (ViewBag.News != null)
        {
            foreach (var item in ViewBag.News)
            {
                <div class="card mb-1" style="width: 18rem;">
                    <img class="card-img-top" src="@item.UrlToImage" alt="Card image cap">
                    <div class="card-body">
                        <h5 class="card-title">@item.Title</h5>
                        <a href="@item.Url" class="btn btn-primary">Go to link</a>
                    </div>
                </div>
            }
        }
    </div>
    <div class="col-md-9">

        <div class="text-center">
            <h1 class="display-4">Welcome</h1>
            <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
        </div>

    </div>

</div>


@section scripts{
    <script src="~/js/LoginBtn.js"></script>
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial")}
}